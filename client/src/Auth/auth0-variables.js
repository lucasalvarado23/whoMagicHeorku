var {domain} = require('./keys');
var {clientId} = require('./keys');
var {callBackURL} = require('./keys');


export const AUTH_CONFIG = {
  domain: domain,
  clientId: clientId,
  callbackUrl: callBackURL
}
